{
  "name": "wermote",
  "private": true,
  "version": "0.1.0",
  "license": "GPL-3.0",
  "scripts": {
    "build": "lerna run build --stream",
    "build:single": "lerna run build --stream --scope"
  },
  "workspaces": [
    "libs/*",
    "apps/*",
    "microservices/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
